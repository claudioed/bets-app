{{- if .Values.kong.enabled -}}
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: rl-by-header
config:
  minute: 5
  limit_by: client_ip
  policy: local
plugin: rate-limiting
{{end}}